ktor {
    development = true

    deployment {
        port = 4000
        port = ${?PORT}
        watch = [ bill-ktor ]
    }

    hikariConfig = "/db.properties"

    application {
        modules = [ cn.fadinglight.ApplicationKt.module ]
    }
}